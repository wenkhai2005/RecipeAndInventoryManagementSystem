import { ExpiredInventoriesPipe } from './expired-inventory-pipe';

describe('ExpiredOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiredInventoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
